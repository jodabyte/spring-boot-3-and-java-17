application:
  version: '@project.version@'

spring:
  application:
    name: '@project.name@'
  output:
    ansi:
      enabled: always
  data:
    mongodb:
      host: localhost
      database: test-data